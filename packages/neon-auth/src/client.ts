/**
 * Vite / React shell — Neon Auth client SDK.
 * Do not import from `@repo/auth` (Clerk).
 */
export { createAuthClient } from "@neondatabase/auth";
export { BetterAuthReactAdapter } from "@neondatabase/auth/react";
